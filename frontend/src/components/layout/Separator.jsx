import React, { useState } from 'react'

const Separator = () => {

    return (
        <span
            className='w-full min-h-0.5 rounded-full bg-accent'
        >
        </span>
    )
}

export default Separator