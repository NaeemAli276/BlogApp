import React from 'react'

const ImageInput = ({ name, value, onChange }) => {
    
    const fileInputRef = null

    return (
        <div
            className='w-full h-full relative flex flex-col gap-2'
        >
            <h1
                className=''
            >

            </h1>
            
        </div>
    )
}

export default ImageInput